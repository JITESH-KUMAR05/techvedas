{
  "name": "techvedas_backend",
  "version": "1.0.0",
  "description": "this is  the backend for techvedas",
  "main": "index.js",
  "scripts": {
    "start": "node --watch index.js",
    "dev": "concurrently \"nodemon app.js\" \"venv\\Scripts\\activate && python app.py\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jitesh Kumar",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.9.2"
  },
  "devDependencies": {
    "concurrently": "^6.5.1",
    "nodemon": "^2.0.12"
  }
}
